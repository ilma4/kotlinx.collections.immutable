package implementations.immutableList

class PersistentVectorBuilderTest{
//    @FuzzTest(maxDuration = "30s")
//    fun lol(list: PersistentVectorBuilder<Int>?) = ignoreExceptions {
//        if (list == null) return
//        val chunked = list.chunked(10)
//        assertTrue(chunked.isNotEmpty())
//    }
}